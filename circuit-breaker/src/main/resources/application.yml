spring:
  application:
    name: hystrix

server:
  port: 8070

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defultZone: http://localhost:8761/eureka/
#      defaultZone: http://10.11.2.20:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
